version: '2'
services:
        www:
                build: .
                container_name: www
                networks:
                        - spider_blogspider
                expose:
                        - "8000"
                external_links:
                        - spider
                        - mongodb
                command: su -m www -c "uwsgi --ini-paste uwsgi.ini"

        nginx:
                image: nginx
                container_name: nginx
                depends_on:
                        - www
                volumes:
                        - ./nginx/nginx.conf:/etc/nginx/nginx.conf
                        - ./nginx/www.conf:/etc/nginx/www.conf
                        - ./static/:/www/static/:ro
                        - ./fonts/:/www/fonts:ro
                networks:
                        - spider_blogspider
                ports:
                        - "80:80"
                        - "443:443"

networks:
        spider_blogspider:
                external: true
